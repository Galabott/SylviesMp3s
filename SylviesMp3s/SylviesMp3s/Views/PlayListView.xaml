<UserControl x:Class="SylviesMp3s.Views.PlayListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SylviesMp3s.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <WrapPanel Orientation="Horizontal">
        <ListBox  Name="SONGS" DisplayMemberPath="Title" Height="339" Width="278" ItemsSource="{Binding Songs, Mode=TwoWay}" SelectedItem="{Binding SelectedSong, Mode=TwoWay}"/>
        <WrapPanel Orientation="Vertical">
            <Button Content="Add a song" Command="{Binding AddTunesCommand}" />
            <Button Content="Remove selected song" Command="{Binding DelTunesCommand}" />
            <Button Content="Modify selected song" Command="{Binding UpdateTunesCommand}" />
            <Button Content="Save Changes" Command="{Binding SaveTunesCommand}" IsEnabled="False"  />
            <Button Content="Cancel Changes" Command="{Binding CancelTunesCommand}" IsEnabled="False" />
        </WrapPanel>
    </WrapPanel>
</UserControl>
